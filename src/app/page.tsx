"use client"
import { createUser } from '@/lib/actions/user.actions'
import React from 'react'

function page() {
    const handle=()=>{
        
    }
 
  return (
    <div>page

        <button onClick={handle}>
            genrate
        </button>
    </div>
  )
}

export default page